# Phase 1 부하테스트 결과

**실행일:** 2026-02-25
**도구:** k6 v1.6.1
**서버 환경:** Docker Compose (load 프로파일)

---

## 테스트 환경

| 항목 | 설정값 |
|------|--------|
| 아키텍처 | **단일 server** (REST + STOMP + MySQL) |
| 서버 실행 방식 | `java -jar app.jar` (Docker 컨테이너) |
| DB | MySQL 8.0 (Docker Compose) |
| 메시지 저장 | MySQL (JPA) |
| 읽음 처리 | MySQL |
| 세션 관리 | HttpSession |
| STOMP 브로커 | SimpleBroker (인메모리) |
| JVM 옵션 | `-Xms512m -Xmx2g -XX:+UseG1GC -XX:MaxGCPauseMillis=200` |
| 컨테이너 CPU 제한 | 2코어 |
| 컨테이너 메모리 제한 | 2.5GB |
| Tomcat 스레드풀 max | 400 |
| HikariCP 커넥션풀 max | 50 |
| Spring 로그 레벨 | WARN (SQL 로깅 비활성화) |
| Spring 프로파일 | `load` |

---

## 테스트 1: WebSocket / STOMP 기능 검증 (websocket-stomp)

**스크립트:** `tests/load/websocket-stomp.js`
**시나리오:** 공유 방 2개, VU 0→200→0 (총 1분)
**목적:** 동시 200 VU 조건에서 WebSocket STOMP 기능 정상 동작 확인

### 결과 요약

| 지표 | 값 | 임계값 | 판정 |
|------|----|--------|------|
| HTTP 응답시간 p(95) | **723.5ms** | < 500ms | ❌ 초과 |
| HTTP 응답시간 avg | 216.1ms | — | — |
| HTTP 요청 실패율 | **0.00%** | < 1% | ✅ 통과 |
| WebSocket 세션 p(95) | **5009.6ms** | < 5000ms | ❌ 초과 (14ms 초과) |
| WebSocket 세션 avg | 3300.8ms | — | — |
| WebSocket 성공률 | **100.0%** | > 98% | ✅ 통과 |
| WS 연결 오류 수 | **0** | < 10 | ✅ 통과 |
| STOMP 연결 오류 수 | **0** | < 10 | ✅ 통과 |
| 메시지 전송 수 | 10,545건 | — | — |
| 메시지 수신 수 | 212,578건 | — | — |
| 총 HTTP 요청 수 | 8,299건 | — | — |

---

## 테스트 2: 단일 방 스트레스 (stress-single-room)

**스크립트:** `tests/load/stress-single-room.js`
**시나리오:** 방 1개, VU 0→50→100→200→350→500→0 (총 4분 30초)

### 결과 요약

| 지표 | 값 | 임계값 | 판정 |
|------|----|--------|------|
| HTTP 응답시간 p(95) | **5086.4ms** | < 3000ms | ❌ 초과 |
| HTTP 요청 실패율 | **0.00%** | < 5% | ✅ 통과 |
| WebSocket 성공률 | **83.9%** | > 80% | ✅ 통과 |
| STOMP 연결 오류 수 | **860** | < 50 | ❌ 초과 |
| 메시지 전송 수 | 13,445건 | — | — |

---

## 테스트 3: 다중 방 스트레스 (stress-multi-room)

**스크립트:** `tests/load/stress-multi-room.js`
**시나리오:** 방 8개, VU 0→100→200→400→0 (총 1분 40초)

### 결과 요약

| 지표 | 값 | 임계값 | 판정 |
|------|----|--------|------|
| HTTP 응답시간 p(95) | **1051.3ms** | < 3000ms | ✅ 통과 |
| HTTP 요청 실패율 | **0.00%** | < 5% | ✅ 통과 |
| WebSocket 성공률 | **100.0%** | > 80% | ✅ 통과 |
| STOMP 연결 오류 수 | **0** | < 50 | ✅ 통과 |

---

## Phase 1 요약

| 항목 | 결과 |
|------|------|
| **안정 VU** | 200 VU (단일 방 기준) |
| **방당 안전 인원** | ~200명 |
| **방당 임계점** | ~350명 (STOMP 오류 발생 시작) |
| **전체 서비스 추정 최대** | 400~800명 |
| **주요 병목** | MySQL 메시지 저장/조회, SimpleBroker 200명/방 한계 |

---

*결과 JSON 파일: `tests/load/results/`*
